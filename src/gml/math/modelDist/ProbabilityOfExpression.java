package gml.math.modelDist;

import gml.math.NormalDistribution;



/**
 * @author steven
 *
 */
public class ProbabilityOfExpression extends NormalDistribution {
	
////	private final static double MATH_SQRT_2 = Math.sqrt(2.0);
//	private final static double MATH_SQRT_2PI = Math.sqrt(2.0 * Math.PI);
//	private final static double LOG_A = Math.log(1.0 / MATH_SQRT_2PI );
////	public class ProbabilityOfExpression extends CauchyDistribution {
//	public static double logPdf(double x, double m, double sd) {
//		x = (x-m)/sd;
//		
////		double a = 1.0 / (MATH_SQRT_2PI );
//		double b = -(x ) * (x ) / (2.0);
//		return LOG_A + b;
//	}

}
